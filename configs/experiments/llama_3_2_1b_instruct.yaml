# Experiment: Llama-3.2-1B-Instruct with QLoRA on WebNLG

run_name: "llama_3_2_1b_qlora_webnlg_3_epochs_${now:%Y-%m-%d_%H-%M-%S}"

model:
  # This is the 4-bit quantized version of the 1B model, optimized for Unsloth.
  name: "unsloth/llama-3.2-1b-instruct-bnb-4bit"
  name_sanitized: "llama-3-2-1b-instruct"
  max_seq_length: 2048
  dtype: null
  load_in_4bit: true

peft:
  # A smaller rank is often sufficient for smaller models.
  r: 16
  lora_alpha: 32
  lora_dropout: 0
  bias: "none"
  use_gradient_checkpointing: true
